SELECT
    filing_date,
    accession,
    period_of_report,
    document_type,
    issuer_cik, 
    issuer_name, 
    issuer_trading_symbol,
    owner_cik,
    owner_name, 
    is_director,
    is_officer, 
    is_ten_percent_owner,
    is_other,
    officer_title,
    other_text,
    security_title, 
    transaction_date,
    transaction_code,
    transaction_shares,
    transaction_price_per_share,
    acquired_disposed_code,
    shares_owned_after,
    ownership_type
FROM {{ source('silver_form4', 'silver_form4_transactions') }}